@model List<VwCatalogoProducto>

@if (Model == null || Model.Count == 0)
{
    <div class="sin-resultados" style="margin: 1rem 0;">
        <i class="bi bi-bag"></i>
        <h2>No hay productos disponibles</h2>
        <p>Vuelve más tarde o prueba cambiando los filtros.</p>
    </div>
}
else
{
    <div class="productos-grid">

        @foreach (VwCatalogoProducto producto in Model)
        {
            <div class="producto-card"
                 data-precio="@producto.Precio.ToString("0.00", System.Globalization.CultureInfo.InvariantCulture)"
                 data-estrellas="@producto.Estrellas"
                 data-stock="@(producto.Stock ?? 0)">
                <a asp-controller="Productos" asp-action="Details" asp-route-idProducto="@producto.IdProducto" class="producto-link">
                    <div class="producto-image-container">
                        <img src="~/images/productosMain/@producto.ImagenPrincipal" alt="@producto.Modelo" class="producto-image" />
                        @if (producto.Stock < 1)
                        {
                            <div class="badge-agotado">Agotado</div>
                        }
                        else if (producto.Stock < 5)
                        {
                            <div class="badge-pocas">Pocas unidades</div>
                        }
                    </div>

                    <div class="producto-info">
                        <div class="producto-marca">@producto.Marca</div>
                        <h3 class="producto-nombre">@producto.Modelo</h3>

                        <div class="producto-categoria">
                            <i class="bi bi-tag"></i> @producto.Tipo - @producto.Subtipo
                        </div>

                        @if (producto.Estrellas > 0)
                        {
                            <div class="producto-rating">
                                @for (int i = 1; i <= 5; i++)
                                {
                                    if (i <= producto.Estrellas)
                                    {
                                        <i class="bi bi-star-fill"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-star"></i>
                                    }
                                }
                                <span class="rating-value">@producto.Estrellas</span>
                            </div>
                        }

                        <div class="producto-precio">@producto.Precio.ToString("C")</div>

                        <div class="especificaciones-producto">
                            @if (producto.Especificaciones != null)
                            {
                                foreach (VwEspecificacionesProducto spec in producto.Especificaciones)
                                {
                                    <ul>
                                        <li>@spec.Especificacion: @spec.Valor</li>
                                    </ul>
                                }
                            }
                        </div>
                    </div>
                </a>
                @if (Context.User.FindFirst("Admin") != null)
                {
                    <a asp-controller="Productos" asp-action="Editar" asp-route-idProducto="@producto.IdProducto"
                       class="admin-btn admin-btn--lg admin-btn--edit producto-card__admin-edit">
                        <i class="bi bi-pencil"></i> Editar
                    </a>
                }
            </div>
        }
    </div>
}
